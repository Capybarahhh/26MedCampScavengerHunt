# .dev/ — developer-only materials

Nothing in here is part of the running game or the deployed site.

- `legacy/` — the original single-file DC-artifact version (`MNEMO.dc.html` +
  its generated `support.js` runtime). Kept as the visual/behavioral reference
  for the Vite+React rewrite. Open the HTML file directly in a browser to
  compare against the current app. Safe to delete once nobody needs the
  side-by-side comparison anymore.
- Anything else dropped here (debug screenshots, scratch notes, one-off
  scripts) is temporary working material — treat it as disposable.
